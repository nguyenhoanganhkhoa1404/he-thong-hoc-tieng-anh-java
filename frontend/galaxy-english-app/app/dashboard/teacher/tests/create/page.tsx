"use client";
import TestEditorPage from "../edit/page";

// Simple wrapper for creation mode — now points to the same component which handles empty ID
export default function CreateTestPage() {
  return <TestEditorPage />;
}
